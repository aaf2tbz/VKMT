diff --git a/dlls/mscoree/mscoree_main.c b/dlls/mscoree/mscoree_main.c
index d65cc25993cfb626c99a1077ba30f70f73ceb7a1..326cde86a71017a13c0f5bcd6fddc9470d4a30ed 100644
--- a/dlls/mscoree/mscoree_main.c
+++ b/dlls/mscoree/mscoree_main.c
@@ -498,6 +498,13 @@ HRESULT WINAPI LoadLibraryShim( LPCWSTR szDllName, LPCWSTR szVersion, LPVOID pvR
 
     *phModDll = LoadLibraryW(dll_filename);
 
+#ifdef __aarch64__
+    /* Framework64 contains x86_64 fake DLLs in the unified prefix.  Native
+     * ARM64 mscoree must fall back to the ARM64 builtin in system32. */
+    if (!*phModDll)
+        *phModDll = LoadLibraryW(szDllName);
+#endif
+
     return *phModDll ? S_OK : E_HANDLE;
 }
 
diff --git a/dlls/ntdll/loader.c b/dlls/ntdll/loader.c
index 6c84e92522f21b66bf949694d87ea13c1edebf0f..7f2e7d71ec841322e6c2e4327f7f866f3917bf20 100644
--- a/dlls/ntdll/loader.c
+++ b/dlls/ntdll/loader.c
@@ -2556,7 +2556,8 @@ static BOOL is_com_ilonly( HANDLE file, const SECTION_IMAGE_INFORMATION *info )
                                       &cor_header, sizeof(cor_header), &magic );
 
     if (len != sizeof(cor_header)) return FALSE;
-    if (magic != IMAGE_NT_OPTIONAL_HDR32_MAGIC) return FALSE;
+    if (magic != IMAGE_NT_OPTIONAL_HDR32_MAGIC &&
+        magic != IMAGE_NT_OPTIONAL_HDR64_MAGIC) return FALSE;
     return !!(cor_header.Flags & COMIMAGE_FLAGS_ILONLY);
 }
 
@@ -4348,7 +4349,7 @@ static void open_known_dll_ntdir(void)
 
 #ifdef __arm64ec__
 
-static void load_arm64ec_module(void)
+static void load_arm64ec_module( const WINE_MODREF *main_module )
 {
     ULONG buffer[16];
     SECTION_IMAGE_INFORMATION image_info;
@@ -4361,10 +4362,13 @@ static void load_arm64ec_module(void)
     HANDLE key;
 
     /* An ARM64X ntdll also services native ARM64/ARM64EC processes.  Only an
-     * AMD64 main image requires the ARM64EC emulator provider. */
+     * AMD64 main image containing native code requires the ARM64EC emulator
+     * provider.  IL-only images enter through native mscoree below; starting
+     * xtajit first would hand its ARM64 _CorExeMain entry to the x64 provider. */
     NtQueryInformationProcess( GetCurrentProcess(), ProcessImageInformation,
                                &image_info, sizeof(image_info), NULL );
     if (image_info.Machine != IMAGE_FILE_MACHINE_AMD64) return;
+    if (main_module->ldr.Flags & LDR_COR_ILONLY) return;
     is_amd64_guest_process = TRUE;
 
     InitializeObjectAttributes( &attr, &nameW, OBJ_CASE_INSENSITIVE, 0, NULL );
@@ -4580,7 +4584,7 @@ void loader_init( CONTEXT *context, void **entry )
         wm = build_main_module();
         build_ntdll_module();
 #ifdef __arm64ec__
-        load_arm64ec_module();
+        load_arm64ec_module( wm );
         update_load_config( wm->ldr.DllBase );
 #endif
 
diff --git a/dlls/ntdll/unix/loader.c b/dlls/ntdll/unix/loader.c
index 5bb1008fb258c93127fbeb7ee8a2150b09c416f0..7f90131f1bb821cc213b881efe146a034ce1e862 100644
--- a/dlls/ntdll/unix/loader.c
+++ b/dlls/ntdll/unix/loader.c
@@ -1442,11 +1442,24 @@ static NTSTATUS open_main_image( UNICODE_STRING *nt_name, void **module, SECTION
     if (!status)
     {
         status = virtual_map_module( mapping, module, &size, info, 0, 0, machine );
-        if (status == STATUS_IMAGE_MACHINE_TYPE_MISMATCH && info->ComPlusNativeReady)
+        if (status == STATUS_IMAGE_MACHINE_TYPE_MISMATCH &&
+            (info->ComPlusNativeReady ||
+             (info->ComPlusILOnly &&
+              is_machine_64bit( info->Machine ) &&
+              is_machine_64bit( native_machine ))))
         {
             info->Machine = native_machine;
             status = STATUS_SUCCESS;
         }
+        else if (NT_SUCCESS(status) && info->ComPlusILOnly &&
+                 is_machine_64bit( info->Machine ) &&
+                 is_machine_64bit( native_machine ))
+        {
+            /* PE32+ IL-only images contain no guest instructions.  Run them
+             * with the native 64-bit CLR instead of constructing an emulated
+             * process/thread contract from the PE header machine. */
+            info->Machine = native_machine;
+        }
         NtClose( mapping );
     }
     else if (status == STATUS_INVALID_IMAGE_NOT_MZ && loadorder != LO_NATIVE)
diff --git a/dlls/ntdll/unix/process.c b/dlls/ntdll/unix/process.c
index 418f95b7735fbfc8b7bbc6f07536635166b41274..1d83661be2ed38d7d81b0b528c23df5b06c953d0 100644
--- a/dlls/ntdll/unix/process.c
+++ b/dlls/ntdll/unix/process.c
@@ -770,7 +770,15 @@ NTSTATUS WINAPI NtCreateUserProcess( HANDLE *process_handle_ptr, HANDLE *thread_
         }
         goto done;
     }
-    if (!machine)
+    if ((pe_info.image_flags & IMAGE_FLAGS_ComPlusILOnly) &&
+        is_machine_64bit( pe_info.machine ) &&
+        is_machine_64bit( native_machine ) &&
+        (!machine || machine == pe_info.machine))
+    {
+        machine = native_machine;
+        pe_info.machine = machine;
+    }
+    else if (!machine)
     {
         machine = pe_info.machine;
         if (is_arm64ec() && pe_info.is_hybrid && machine == IMAGE_FILE_MACHINE_ARM64)
diff --git a/dlls/ntdll/unix/virtual.c b/dlls/ntdll/unix/virtual.c
index 8369399f942b844fd41a7d518035337d76724f79..d97e9f650b143f9ab66491df38671ae0072c0d3c 100644
--- a/dlls/ntdll/unix/virtual.c
+++ b/dlls/ntdll/unix/virtual.c
@@ -820,22 +820,39 @@ static void reserve_wow64_host_arena(void)
     for (i = 0; i < ARRAY_SIZE(candidates); i++)
     {
         void *base = (void *)candidates[i];
+        void *null_page = base;
         void *dos = (char *)base + 0x110000;
         void *image = (char *)base + 0x400000;
         void *kuser = (char *)base + 0x7ffe0000;
 
-        if (anon_mmap_tryfixed( dos, 0x110000, PROT_NONE, MAP_NORESERVE ) == MAP_FAILED) continue;
+        /* Guest VA zero is the NT NULL-pointer sentinel.  If the biased host
+         * address at the start of the arena is left free, an ordinary
+         * base-NULL allocation can land there.  Its valid reverse conversion
+         * is then indistinguishable from conversion failure, and registering
+         * it can replace the bootstrap guest-zero aperture record. */
+        if (anon_mmap_tryfixed( null_page, 0x10000, PROT_NONE, MAP_NORESERVE ) == MAP_FAILED) continue;
+        if (anon_mmap_tryfixed( dos, 0x110000, PROT_NONE, MAP_NORESERVE ) == MAP_FAILED)
+        {
+            munmap( null_page, 0x10000 );
+            continue;
+        }
         if (anon_mmap_tryfixed( image, 0x4000000, PROT_NONE, MAP_NORESERVE ) == MAP_FAILED)
         {
+            munmap( null_page, 0x10000 );
             munmap( dos, 0x110000 );
             continue;
         }
         if (anon_mmap_tryfixed( kuser, host_page_size, PROT_NONE, MAP_NORESERVE ) == MAP_FAILED)
         {
+            munmap( null_page, 0x10000 );
             munmap( dos, 0x110000 );
             munmap( image, 0x4000000 );
             continue;
         }
+        /* Do not add this mapping to Wine's reusable reserved-area list:
+         * doing so lets NtAllocateVirtualMemory commit it for a base-NULL
+         * request.  It must remain an unavailable host guard for the lifetime
+         * of the process. */
         mmap_add_reserved_area( dos, 0x110000 );
         mmap_add_reserved_area( image, 0x4000000 );
         mmap_add_reserved_area( kuser, host_page_size );
@@ -3486,7 +3503,10 @@ static NTSTATUS map_image_into_view( struct file_view *view, const UNICODE_STRIN
             update_arm64ec_ranges( view, nt, dir, &image_info->entry_point );
     }
 #endif
-    if (machine && machine != nt->FileHeader.Machine)
+    if (machine && machine != nt->FileHeader.Machine &&
+        !((image_info->image_flags & IMAGE_FLAGS_ComPlusILOnly) &&
+          is_machine_64bit( machine ) &&
+          is_machine_64bit( nt->FileHeader.Machine )))
     {
         status = STATUS_NOT_SUPPORTED;
         goto done;
diff --git a/dlls/wow64/file.c b/dlls/wow64/file.c
index 68cb706ac9ba132de655936002892ab853bf28a3..ef63ca3868101cfbf95b071c274f215661eb97ac 100644
--- a/dlls/wow64/file.c
+++ b/dlls/wow64/file.c
@@ -31,6 +31,7 @@
 
 WINE_DEFAULT_DEBUG_CHANNEL(wow);
 
+#define IOCTL_AFD_GET_EVENTS_WOW64 CTL_CODE(FILE_DEVICE_BEEP, 0x822, METHOD_NEITHER, FILE_ANY_ACCESS)
 
 static FILE_OBJECTID_BUFFER windir_id, sysdir_id;
 
@@ -381,14 +382,24 @@ NTSTATUS WINAPI wow64_NtDeviceIoControlFile( UINT *args )
     ULONG apc_param = get_ulong( &args );
     IO_STATUS_BLOCK32 *io32 = get_ptr( &args );
     ULONG code = get_ulong( &args );
-    void *in_buf = get_ptr( &args );
+    ULONG in_buf32 = get_ulong( &args );
     ULONG in_len = get_ulong( &args );
     void *out_buf = get_ptr( &args );
     ULONG out_len = get_ulong( &args );
 
+    void *in_buf;
     IO_STATUS_BLOCK io;
     NTSTATUS status;
 
+    /* AFD encodes the optional reset event handle in the input-buffer value
+     * for IOCTL_AFD_GET_EVENTS while keeping the input length at zero.  It is
+     * not a guest pointer and must not be translated through the guest-memory
+     * map. */
+    if (code == IOCTL_AFD_GET_EVENTS_WOW64 && !in_len)
+        in_buf = (void *)(ULONG_PTR)in_buf32;
+    else
+        in_buf = wow64_guest_to_host_ptr( in_buf32, in_len ? in_len : 1 );
+
     status = NtDeviceIoControlFile( handle, event, apc_32to64( apc ), apc_param_32to64( apc, apc_param ),
                                     iosb_32to64( &io, io32 ), code, in_buf, in_len, out_buf, out_len );
     put_iosb( io32, &io );
diff --git a/dlls/wow64/memory.c b/dlls/wow64/memory.c
index 356309d1852c7067082fd49d7f54dd089aa1ea6d..98cac714ed09d5856bc2e4324d4b51e2fd4ea11f 100644
--- a/dlls/wow64/memory.c
+++ b/dlls/wow64/memory.c
@@ -55,9 +55,9 @@ void wow64_register_guest_memory( ULONG guest, void *host, SIZE_T size )
     RtlAcquireSRWLockExclusive( &guest_map_lock );
     for (i = 0; i < ARRAY_SIZE(guest_maps); i++)
     {
-        if (guest_maps[i].host == host && guest_maps[i].guest == guest)
+        if (guest_maps[i].host == host && guest_maps[i].guest == guest &&
+            guest_maps[i].size == size)
         {
-            guest_maps[i].size = size;
             if (pBTCpuMapGuestMemory) pBTCpuMapGuestMemory( guest, host, size );
             RtlReleaseSRWLockExclusive( &guest_map_lock );
             return;
@@ -84,7 +84,11 @@ void wow64_unregister_guest_memory( ULONG guest, SIZE_T size )
     unsigned int i, j;
 
     RtlAcquireSRWLockExclusive( &guest_map_lock );
-    for (i = 0; i < ARRAY_SIZE(guest_maps); i++)
+    /* Concrete mappings are appended over the four bootstrap aperture
+     * records.  Remove the newest exact mapping first so releasing a view at
+     * an aperture chunk boundary cannot accidentally discard its underlying
+     * compatibility mapping. */
+    for (i = ARRAY_SIZE(guest_maps); i-- > 0; )
     {
         if (guest_maps[i].guest == guest && (!size || guest_maps[i].size == size))
         {
diff --git a/dlls/wow64/process.c b/dlls/wow64/process.c
index c9d9c1c39bbc14c65fc6d2eec9dd0d227910a469..f7ae3d2991f31db5a7b162b708e17b3d62360059 100644
--- a/dlls/wow64/process.c
+++ b/dlls/wow64/process.c
@@ -59,6 +59,15 @@ static BOOL is_process_id_wow64( const CLIENT_ID *id )
     return ret;
 }
 
+static BOOL is_current_process_thread( HANDLE handle )
+{
+    THREAD_BASIC_INFORMATION info;
+
+    if (NtQueryInformationThread( handle, ThreadBasicInformation, &info, sizeof(info), NULL ))
+        return FALSE;
+    return info.ClientId.UniqueProcess == ULongToHandle( GetCurrentProcessId() );
+}
+
 
 static RTL_USER_PROCESS_PARAMETERS *process_params_32to64( RTL_USER_PROCESS_PARAMETERS **params,
                                                            RTL_USER_PROCESS_PARAMETERS32 *params32 )
@@ -1118,6 +1127,23 @@ NTSTATUS WINAPI wow64_NtSuspendThread( UINT *args )
     HANDLE handle = get_handle( &args );
     ULONG *count = get_ptr( &args );
 
+    /*
+     * A local emulated thread may be executing entirely inside the native
+     * CPU provider.  Let that provider pause its dispatcher, spill the live
+     * guest registers into Wine's WOW64 CPU area, and synchronize the native
+     * stop before an i386 caller reads the thread context.  Directly asking
+     * the server to suspend first leaves the CPU area stale, which makes
+     * HotSpot consume an invalid EIP/ESP and root map at a GC safepoint.
+     *
+     * Remote-process threads do not belong to this provider instance and
+     * retain the ordinary Wine server path.
+     */
+    if (pBTCpuSuspendLocalThread && is_current_process_thread( handle ))
+    {
+        TRACE( "routing local thread %p through CPU provider suspend\n", handle );
+        return pBTCpuSuspendLocalThread( handle, count );
+    }
+    TRACE( "routing thread %p through native server suspend\n", handle );
     return NtSuspendThread( handle, count );
 }
 
diff --git a/dlls/wow64/sync.c b/dlls/wow64/sync.c
index f8ba5b647d5b8dd10266808ea184d14967ce0913..0162f84313593a4cf8a3a60fd877fa4a42bc55bd 100644
--- a/dlls/wow64/sync.c
+++ b/dlls/wow64/sync.c
@@ -1793,9 +1793,17 @@ NTSTATUS WINAPI wow64_NtWaitForMultipleObjects( UINT *args )
 
     HANDLE handles[MAXIMUM_WAIT_OBJECTS];
     DWORD i;
+    NTSTATUS status;
 
     for (i = 0; i < count && i < MAXIMUM_WAIT_OBJECTS; i++) handles[i] = LongToHandle( handles_ptr[i] );
-    return NtWaitForMultipleObjects( count, handles, type, alertable, timeout );
+    status = NtWaitForMultipleObjects( count, handles, type, alertable, timeout );
+
+    /*
+     * HotSpot's observed code-cache publication boundary is its non-alertable
+     * single-object wait below.  Do not broaden synchronization to arbitrary
+     * multi-object waits; callback and APC dispatch can still be nested there.
+     */
+    return status;
 }
 
 
@@ -1808,7 +1816,16 @@ NTSTATUS WINAPI wow64_NtWaitForSingleObject( UINT *args )
     BOOLEAN alertable = get_ulong( &args );
     const LARGE_INTEGER *timeout = get_ptr( &args );
 
-    return NtWaitForSingleObject( handle, alertable, timeout );
+    NTSTATUS status = NtWaitForSingleObject( handle, alertable, timeout );
+
+    /*
+     * An alertable wait resumes through APC dispatch and its provider
+     * call-return state is still nested until the dispatcher returns.
+     * Synchronize only ordinary waits; HotSpot's VM/GC publication waits are
+     * non-alertable.
+     */
+    if (!alertable && pBTCpuSynchronizeDynamicCode) pBTCpuSynchronizeDynamicCode();
+    return status;
 }
 
 
diff --git a/dlls/wow64/syscall.c b/dlls/wow64/syscall.c
index 1b13b5b76ebefa8a15dc2d4c5534374c28cd2426..b97cf3707d38aa654511705555f021a2be12381d 100644
--- a/dlls/wow64/syscall.c
+++ b/dlls/wow64/syscall.c
@@ -119,6 +119,7 @@ static void *   (WINAPI *pKiUserApcDispatcher)(void);
 static void *   (WINAPI *pKiUserCallbackDispatcher)(void);
 void     (WINAPI *pBTCpuFlushInstructionCache2)( const void *, SIZE_T ) = NULL;
 void     (WINAPI *pBTCpuFlushInstructionCacheHeavy)( const void *, SIZE_T ) = NULL;
+void     (WINAPI *pBTCpuSynchronizeDynamicCode)( void ) = NULL;
 NTSTATUS (WINAPI *pBTCpuNotifyMapViewOfSection)( void *, void *, void *, SIZE_T, ULONG, ULONG ) = NULL;
 void     (WINAPI *pBTCpuNotifyMemoryAlloc)( void *, SIZE_T, ULONG, ULONG, BOOL, NTSTATUS ) = NULL;
 void     (WINAPI *pBTCpuNotifyMemoryDirty)( void *, SIZE_T ) = NULL;
@@ -132,6 +133,7 @@ NTSTATUS (WINAPI *pBTCpuResetToConsistentState)( EXCEPTION_POINTERS * ) = NULL;
 void     (WINAPI *pBTCpuUpdateProcessorInformation)( SYSTEM_CPU_INFORMATION * ) = NULL;
 void     (WINAPI *pBTCpuProcessTerm)( HANDLE, BOOL, NTSTATUS ) = NULL;
 void     (WINAPI *pBTCpuThreadTerm)( HANDLE, LONG ) = NULL;
+NTSTATUS (WINAPI *pBTCpuSuspendLocalThread)( HANDLE, ULONG * ) = NULL;
 
 BOOL WINAPI DllMain( HINSTANCE inst, DWORD reason, void *reserved )
 {
@@ -1054,6 +1056,7 @@ static DWORD WINAPI process_init( RTL_RUN_ONCE *once, void *param, void **contex
     GET_PTR( BTCpuUserCallbackReturn );
     GET_PTR( BTCpuFlushInstructionCache2 );
     GET_PTR( BTCpuFlushInstructionCacheHeavy );
+    GET_PTR( BTCpuSynchronizeDynamicCode );
     GET_PTR( BTCpuNotifyMapViewOfSection );
     GET_PTR( BTCpuNotifyMemoryAlloc );
     GET_PTR( BTCpuNotifyMemoryDirty );
@@ -1065,6 +1068,7 @@ static DWORD WINAPI process_init( RTL_RUN_ONCE *once, void *param, void **contex
     GET_PTR( BTCpuUnmapGuestMemory );
     GET_PTR( BTCpuUpdateProcessorInformation );
     GET_PTR( BTCpuProcessTerm );
+    GET_PTR( BTCpuSuspendLocalThread );
     GET_PTR( BTCpuThreadTerm );
     GET_PTR( __wine_get_unix_opcode );
 
diff --git a/dlls/wow64/wow64_private.h b/dlls/wow64/wow64_private.h
index 5b2406dc8c3584f8234a1cf3203653dfebb3251d..e8d77a72ae5d09b827b502095f857a75a7ca8586 100644
--- a/dlls/wow64/wow64_private.h
+++ b/dlls/wow64/wow64_private.h
@@ -56,6 +56,7 @@ extern SYSTEM_DLL_INIT_BLOCK *pLdrSystemDllInitBlock;
 
 extern void     (WINAPI *pBTCpuFlushInstructionCache2)( const void *, SIZE_T );
 extern void     (WINAPI *pBTCpuFlushInstructionCacheHeavy)( const void *, SIZE_T );
+extern void     (WINAPI *pBTCpuSynchronizeDynamicCode)( void );
 extern NTSTATUS (WINAPI *pBTCpuNotifyMapViewOfSection)( void *, void *, void *, SIZE_T, ULONG, ULONG );
 extern void     (WINAPI *pBTCpuNotifyMemoryAlloc)( void *, SIZE_T, ULONG, ULONG, BOOL, NTSTATUS );
 extern void     (WINAPI *pBTCpuNotifyMemoryDirty)( void *, SIZE_T );
@@ -67,6 +68,7 @@ extern void     (WINAPI *pBTCpuMapGuestMemory)( ULONG, void *, SIZE_T );
 extern void     (WINAPI *pBTCpuUnmapGuestMemory)( ULONG, SIZE_T );
 extern void     (WINAPI *pBTCpuUpdateProcessorInformation)( SYSTEM_CPU_INFORMATION * );
 extern void     (WINAPI *pBTCpuProcessTerm)( HANDLE, BOOL, NTSTATUS );
+extern NTSTATUS (WINAPI *pBTCpuSuspendLocalThread)( HANDLE, ULONG * );
 extern void     (WINAPI *pBTCpuThreadTerm)( HANDLE, LONG );
 
 struct object_attr64
diff --git a/dlls/xtajit64/cpu.c b/dlls/xtajit64/cpu.c
index 7aa9e15d850b807e383f5f1e6a6872757ef8c156..f7aabc2791d83f462a4964ac6bcc77d5c2ed4d8e 100644
--- a/dlls/xtajit64/cpu.c
+++ b/dlls/xtajit64/cpu.c
@@ -112,6 +112,10 @@ void WINAPI NotifyMemoryAlloc( void *addr, SIZE_T size, ULONG type, ULONG prot,
 void WINAPI NotifyMemoryFree( void *addr, SIZE_T size, ULONG type, BOOL is_post, NTSTATUS status )
 {
     TRACE( "%p %Ix type %lx is_post %d status %lx\n", addr, size, type, is_post, status );
+    /* Invalidate before a successful free makes the guest bytes unreadable.
+     * MEM_RELEASE commonly reports a zero size, which deliberately requests
+     * a conservative whole-cache retirement. */
+    if (!is_post) vkmt_jit_invalidate( addr, size );
 }
 
 
@@ -131,6 +135,7 @@ void WINAPI NotifyMemoryProtect( void *addr, SIZE_T size, ULONG prot, BOOL is_po
 void WINAPI NotifyUnmapViewOfSection( void *addr, BOOL is_post, NTSTATUS status )
 {
     TRACE( "%p is_post %d status %lx\n", addr, is_post, status );
+    if (!is_post) vkmt_jit_invalidate( NULL, 0 );
 }
 
 
diff --git a/dlls/xtajit64/vkmt/dispatch.c b/dlls/xtajit64/vkmt/dispatch.c
index 17cfe4febf84d7404cc9b53cdc0db1314f097fb9..5a1469d2c7f87630224034c885fce2e721e88645 100644
--- a/dlls/xtajit64/vkmt/dispatch.c
+++ b/dlls/xtajit64/vkmt/dispatch.c
@@ -453,7 +453,15 @@ void vkmt_reset_to_consistent_state( EXCEPTION_RECORD *rec, CONTEXT *context, AR
     TRACE( "reconciling exception %lx to guest rip %p (host pc was %p)\n",
            rec ? rec->ExceptionCode : 0, (void *)ctx->rip, (void *)ec->Pc );
 
-    if (rec) rec->ExceptionAddress = (void *)ctx->rip;
+    if (rec)
+    {
+        if (ctx->pending_exception_code)
+        {
+            rec->ExceptionCode = ctx->pending_exception_code;
+            ctx->pending_exception_code = 0;
+        }
+        rec->ExceptionAddress = (void *)ctx->rip;
+    }
     vkmt_context_to_ec( ec, ctx );
     ec->ContextFlags = CONTEXT_AMD64_ALL;
     ctx->exception_dispatch_context = ec;
diff --git a/dlls/xtajit64/vkmt/interp.c b/dlls/xtajit64/vkmt/interp.c
index d3d8bcb74ac30a84a4430969a28d20d3e0165a0a..f24fa377bd7af1752c2a69f4f76d92daf6823b6a 100644
--- a/dlls/xtajit64/vkmt/interp.c
+++ b/dlls/xtajit64/vkmt/interp.c
@@ -1308,7 +1308,10 @@ static BOOL exec_primary( struct vkmt_x64_context *ctx, const struct vkmt_insn *
             a = get_rm( ctx, insn, size );
             if (!a)
             {
-                giveup_dump( ctx, op, "integer divide by zero (#DE)" );
+                volatile ULONG_PTR *fault = NULL;
+
+                ctx->pending_exception_code = EXCEPTION_INT_DIVIDE_BY_ZERO;
+                (void)*fault;
                 return FALSE;
             }
             if (size == 8)
diff --git a/dlls/xtajit64/vkmt/jit.c b/dlls/xtajit64/vkmt/jit.c
index 9f56b22825b81af2f4baf70a04e7b33d722ba21f..2f971cb54f1067a218011fdc3d50d9d9b23dc938 100644
--- a/dlls/xtajit64/vkmt/jit.c
+++ b/dlls/xtajit64/vkmt/jit.c
@@ -50,11 +50,13 @@ struct vkmt_jit_block
     SIZE_T code_size;
     void *code;
     BYTE direct[VKMT_JIT_MAX_INSNS];
+    BYTE guest_bytes[VKMT_JIT_MAX_INSNS][16];
     struct vkmt_insn insn[VKMT_JIT_MAX_INSNS];
 };
 
 static RTL_CRITICAL_SECTION jit_cs;
 static BOOL jit_ready;
+static BOOL jit_enabled;
 static ULONG jit_epoch;
 static struct vkmt_jit_block *jit_blocks;
 static struct vkmt_jit_block *jit_hash[VKMT_JIT_HASH_SLOTS];
@@ -305,13 +307,34 @@ static BOOL jit_direct_eligible( const struct vkmt_insn *insn )
     return FALSE;
 }
 
+/* x86 HotSpot treats its code cache as instruction-coherent and may recycle
+ * an nmethod without calling FlushInstructionCache.  The ARM64 host cannot
+ * infer that contract from page protections: HotSpot keeps the code heap
+ * executable while patching it.  Validate the exact guest bytes before
+ * entering a cached native block so recycled code can never reuse an old
+ * guest-to-host translation. */
+static BOOL jit_block_unchanged( const struct vkmt_jit_block *block )
+{
+    UINT i;
+
+    for (i = 0; i < block->count; ++i)
+        if (memcmp( block->insn[i].start, block->guest_bytes[i],
+                    block->insn[i].len ))
+            return FALSE;
+    return TRUE;
+}
+
 static struct vkmt_jit_block *jit_find_block( ULONGLONG rip )
 {
     struct vkmt_jit_block *block;
     UINT slot = ((rip >> 4) ^ (rip >> 20)) & (VKMT_JIT_HASH_SLOTS - 1);
 
     for (block = jit_hash[slot]; block; block = block->hash_next)
-        if (block->epoch == jit_epoch && block->rip == rip) return block;
+    {
+        if (block->epoch != jit_epoch || block->rip != rip) continue;
+        if (jit_block_unchanged( block )) return block;
+        block->epoch = 0;
+    }
     return NULL;
 }
 
@@ -336,6 +359,9 @@ static struct vkmt_jit_block *jit_compile( struct vkmt_x64_context *ctx )
         if (i) probe.rip = (ULONG_PTR)(block->insn[i - 1].start + block->insn[i - 1].len);
         vkmt_decode( &probe, &block->insn[i] );
         if (!jit_eligible( &block->insn[i] )) break;
+        if (block->insn[i].len > sizeof(block->guest_bytes[i])) break;
+        memcpy( block->guest_bytes[i], block->insn[i].start,
+                block->insn[i].len );
         block->count++;
         /* Control flow is cached as the final fallback operation.  Keeping
          * it out of the cache forced every hot loop edge through the outer
@@ -433,12 +459,42 @@ fail:
     return NULL;
 }
 
+static BOOL jit_guest_page_requires_interpreter( ULONGLONG rip )
+{
+    MEMORY_BASIC_INFORMATION info;
+
+    if (NtQueryVirtualMemory( GetCurrentProcess(), (void *)(ULONG_PTR)rip,
+                              MemoryBasicInformation, &info, sizeof(info),
+                              NULL ))
+        return TRUE;
+    /* Dynamically generated x86-64 code has no immutable-image contract.
+     * HotSpot can change it through an alias even while this view is RX, so
+     * page protection alone is insufficient.  Keep private executable code
+     * on the precise interpreter; immutable MEM_IMAGE text retains tier-0. */
+    return info.Type == MEM_PRIVATE ||
+           (info.Protect & 0xff) == PAGE_READWRITE ||
+           (info.Protect & 0xff) == PAGE_WRITECOPY ||
+           (info.Protect & 0xff) == PAGE_EXECUTE_READWRITE ||
+           (info.Protect & 0xff) == PAGE_EXECUTE_WRITECOPY;
+}
+
 NTSTATUS vkmt_jit_process_init(void)
 {
     if (!jit_ready)
     {
+        WCHAR setting_buffer[8];
+        UNICODE_STRING name, setting;
+
         RtlInitializeCriticalSection( &jit_cs );
         jit_epoch = 1;
+        jit_enabled = TRUE;
+        RtlInitUnicodeString( &name, L"VKMT_X64_TIER0" );
+        setting.Buffer = setting_buffer;
+        setting.Length = 0;
+        setting.MaximumLength = sizeof(setting_buffer);
+        if (!RtlQueryEnvironmentVariable_U( NULL, &name, &setting ) &&
+            setting.Length >= sizeof(WCHAR) && setting.Buffer[0] == L'0')
+            jit_enabled = FALSE;
         jit_ready = TRUE;
     }
     return STATUS_SUCCESS;
@@ -470,7 +526,21 @@ void vkmt_jit_invalidate( void *addr, SIZE_T size )
     BYTE *first = addr, *last;
     UINT i;
 
-    if (!jit_ready || !addr || !size) return;
+    if (!jit_ready) return;
+    if (!addr || !size)
+    {
+        RtlEnterCriticalSection( &jit_cs );
+        for (block = jit_blocks; block; block = block->next)
+            if (block->epoch == jit_epoch)
+            {
+                block->epoch = 0;
+                retired++;
+            }
+        RtlLeaveCriticalSection( &jit_cs );
+        if (retired) TRACE( "JIT retired %lu blocks after global invalidation\n",
+                            retired );
+        return;
+    }
     if (jit_build_base && (BYTE *)addr < (BYTE *)jit_build_base + jit_build_size &&
         (BYTE *)addr + size > (BYTE *)jit_build_base) return;
     last = first + size;
@@ -508,13 +578,22 @@ UINT vkmt_jit_run_block( struct vkmt_x64_context *ctx )
     struct vkmt_jit_block *block, *linked = NULL;
     UINT total = 0;
 
-    if (!jit_ready || !ctx->rip) return 0;
+    if (!jit_ready || !jit_enabled || !ctx->rip ||
+        jit_guest_page_requires_interpreter( ctx->rip ))
+        return 0;
     for (;;)
     {
         RtlEnterCriticalSection( &jit_cs );
-        if (linked && linked->epoch == jit_epoch && linked->rip == ctx->rip)
+        if (linked && linked->epoch == jit_epoch && linked->rip == ctx->rip &&
+            jit_block_unchanged( linked ))
             block = linked;
-        else block = jit_find_block( ctx->rip );
+        else
+        {
+            if (linked && linked->epoch == jit_epoch &&
+                !jit_block_unchanged( linked ))
+                linked->epoch = 0;
+            block = jit_find_block( ctx->rip );
+        }
         if (!block)
         {
             UINT hot = ((ctx->rip >> 4) ^ (ctx->rip >> 20)) &
diff --git a/dlls/xtajit64/vkmt/vkmt.h b/dlls/xtajit64/vkmt/vkmt.h
index 44787a801d8ba6f97627b456db3bdb07f1a22cfd..e6c020d820142e3cf1a9a27c8f38a053da949fee 100644
--- a/dlls/xtajit64/vkmt/vkmt.h
+++ b/dlls/xtajit64/vkmt/vkmt.h
@@ -83,6 +83,10 @@ struct vkmt_x64_context
     ULONGLONG ec_ret_rip[64];
     ULONGLONG ec_ret_rsp[64];
     UINT      ec_ret_depth;
+    /* A guest fault that cannot be generated directly by AArch64 (notably
+     * x86 #DE) uses a host fault to enter Wine's exception path, then
+     * ResetToConsistentState replaces the record with this guest code. */
+    ULONG     pending_exception_code;
 };
 
 /* context.c */
diff --git a/server/mapping.c b/server/mapping.c
index a53b6b68304b9f815f571897a0f0e9d29c7360e8..5cf7139ac09ca12ae5a8d2b59eae4bc3848d0792 100644
--- a/server/mapping.c
+++ b/server/mapping.c
@@ -1751,6 +1751,8 @@ DECL_HANDLER(map_image_view)
 
     if ((view = mem_alloc( sizeof(*view) )))
     {
+        int native_managed, native_ilonly64;
+
         view->base      = req->base;
         view->size      = req->size;
         view->flags     = mapping->flags;
@@ -1760,16 +1762,20 @@ DECL_HANDLER(map_image_view)
         view->committed = NULL;
         view->shared    = mapping->shared ? (struct shared_map *)grab_object( mapping->shared ) : NULL;
         view->image     = mapping->image;
+        native_ilonly64 = (view->image.image_flags & IMAGE_FLAGS_ComPlusILOnly) &&
+                          is_machine_64bit( view->image.machine ) &&
+                          is_machine_64bit( native_machine );
+        native_managed = (view->image.image_flags & IMAGE_FLAGS_ComPlusNativeReady) ||
+                         native_ilonly64;
         if (add_process_view( current, view ))
         {
             current->entry_point = view->base + req->entry;
-            current->process->machine = (view->image.image_flags & IMAGE_FLAGS_ComPlusNativeReady) ?
-                                         native_machine : req->machine;
+            current->process->machine = native_managed ? native_machine : req->machine;
         }
 
         if (view->base != (mapping->image.map_addr ? mapping->image.map_addr : mapping->image.base) + req->offset)
             set_error( STATUS_IMAGE_NOT_AT_BASE );
-        if (req->machine != current->process->machine)
+        if (!native_ilonly64 && req->machine != current->process->machine)
         {
             /* on 32-bit, the native 64-bit machine is allowed */
             if (is_machine_64bit( current->process->machine ) || req->machine != native_machine)
